Watch action row — the remote controls for the session running on the phone.

```jsx
<WearAction actions={[{icon:"check", label:"Complete set", tone:"primary"}]} />
```

One action on a round face — a two- or three-up row of 48dp buttons crosses the progress arc. Adjusting weight or rest length happens on the phone; the watch confirms the set and skips rest.
