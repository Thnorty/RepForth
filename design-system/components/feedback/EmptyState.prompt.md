Zero-data state. Say what goes here and give exactly one way to fill it.

```jsx
<EmptyState icon="event_note" title="No workouts yet" body="Build one from the catalog, or let RepForth draft it for you."
  action={<Button variant="filled" icon="add">New workout</Button>} />
```
