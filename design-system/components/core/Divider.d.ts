/** Hairline separator in outline-variant. Use sparingly: spacing separates first, lines second. */
export interface DividerProps {
  inset?: boolean;
  vertical?: boolean;
  className?: string;
}
export declare function Divider(props: DividerProps): JSX.Element;
