Compact rounded surface. Never combine `elevated` with `outlined` — tone or shadow, not both.

```jsx
<Card>Plan summary</Card>
<Card variant="outlined" interactive onClick={open}>Push Day A</Card>
<Card flush><img className="rf-media-square" src={media} alt="" /></Card>
```
