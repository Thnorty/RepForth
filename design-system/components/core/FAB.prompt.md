Floating action button — one per screen, bottom-right above the navigation bar.

```jsx
<FAB icon="add" label="New workout" extended />
<FAB icon="play_arrow" label="Resume session" size="large" />
```

Prefer `extended` on phone: the label makes the action unambiguous in both languages.
