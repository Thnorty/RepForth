Rest screen / sheet. Ring + numeral + 64dp skip. Every control is thumb-sized because it is used mid-set.

```jsx
<RestTimer remaining={62} total={90} label="Dinlenme" skipLabel="Dinlenmeyi atla"
  nextUp="Sırada: Incline dumbbell press · Set 2" onSkip={skip} onAdd={plus15} onSubtract={minus15} />
```
