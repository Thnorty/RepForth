Exercise entry with 1:1 media, muscle/equipment chips and its set prescription.

```jsx
<ExerciseCard name="Barbell bench press" sets={4} reps={8}
  tags={[{label:"Chest",icon:"accessibility_new"},{label:"Barbell",icon:"fitness_center"}]}
  trailing={<Icon name="drag_handle" />} />
```

`layout="stacked"` for the two-up catalog grid.
