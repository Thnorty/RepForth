Text / numeric input, 48dp box, label above.

```jsx
<TextField label="Search exercises" icon="search" placeholder="Bench press" />
<TextField label="Weight" numeric suffix="kg" value="82.5" />
<TextField label="Workout name" error="Bu ad zaten kullanılıyor" />
```
