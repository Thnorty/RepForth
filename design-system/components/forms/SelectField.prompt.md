Dropdown for short, closed sets (units, language, rest presets).

```jsx
<SelectField label="Units" icon="straighten" value={u} onChange={set} options={["kg","lb"]} />
<SelectField label="Dil / Language" options={[{value:"en",label:"English"},{value:"tr",label:"Türkçe"}]} />
```
