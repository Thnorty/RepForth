Compact view switch for 2–3 options. Keep labels one or two words — Turkish grows.

```jsx
<SegmentedButtons label="Units" value={u} onChange={setU} options={[{value:"kg",label:"kg"},{value:"lb",label:"lb"}]} />
```
