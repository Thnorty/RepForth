Settings toggle — applies immediately, no confirmation.

```jsx
<Switch label="Keep screen on" description="Antrenman sırasında ekranı açık tut" checked={on} onChange={setOn} />
```

The thumb shows a check when on, so state is never colour-only.
